Offline test fixtures. Regenerate with `python tests/make_fixtures.py`, then
`python scripts/build.py --mock --fixtures tests/fixtures` to exercise the
whole pipeline without network access. Headlines are synthetic.
